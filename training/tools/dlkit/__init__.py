"""dlkit — the Defect Log dataset toolkit.

Pure standard library, so that checking a dataset never needs an install.
Pillow, if it happens to be present, adds the near-duplicate check; nothing
else is optional.
"""
