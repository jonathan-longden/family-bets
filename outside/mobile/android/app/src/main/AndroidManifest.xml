<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Approximate location only, and only to fetch a forecast. The app
         rounds every coordinate to four decimal places before it uses it, so
         a precise fix would be thrown away the moment it arrived.

         This is supported rather than merely tolerated. The Geolocation
         plugin ships an empty manifest and declares no permissions of its
         own; its getAlias() reads which of them this manifest declares and
         picks the coarse-only alias when the precise one is absent. Its own
         "permissions missing" error says as much: at least ACCESS_COARSE_
         LOCATION, and ACCESS_FINE_LOCATION only if you want precise access.

         The explicit removal below is belt and braces against a future plugin
         version, or another plugin, quietly merging the precise permission in.
         The app must never ask for it: the Play data safety form says
         approximate, and that has to stay true. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:node="remove" />

    <!-- Location is optional: the app is fully usable by searching for a town,
         so a phone without location hardware should still see it in the shop. -->
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

</manifest>
