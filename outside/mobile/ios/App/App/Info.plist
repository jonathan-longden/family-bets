<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>Blooming Weather</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0.0</string>
	<key>CFBundleVersion</key>
	<string>6</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- Shown by iOS in its own permission prompt, word for word. It is the
	     only sentence the user gets to judge the request on, so it says what
	     the location is for and stops. -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>To show the forecast where you are. Your coordinates are rounded before they are used, and go only to the weather service — never to us.</string>
	<!-- Not a permission the app asks for itself: it is what iOS requires when
	     somebody picks "Save Image" from the share sheet this app raises.
	     Without it, that tap crashes the app. -->
	<!-- Ask for reduced accuracy from the start. iOS then shows "Precise: Off"
	     in the prompt and hands back a fuzzed position, which is all this app
	     wants: it rounds the coordinates itself before using them. -->
	<key>NSLocationDefaultAccuracyReduced</key>
	<true/>
	<key>NSPhotoLibraryAddUsageDescription</key>
	<string>To save a weather card to your photos when you choose Save Image.</string>
	<!-- The app makes ordinary HTTPS requests and contains no other
	     cryptography, which is exempt. Declaring it here answers the export
	     compliance question once instead of at every single upload. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
    <false/>
    <key>UISceneConfigurations</key>
    <dict>
      <key>UIWindowSceneSessionRoleApplication</key>
      <array>
        <dict>
            <key>UISceneConfigurationName</key>
            <string>Default Configuration</string>
            <key>UISceneDelegateClassName</key>
            <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
            <key>UISceneStoryboardFile</key>
            <string>Main</string>
        </dict>
      </array>
    </dict>
	</dict>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<!-- Portrait only. The screen is one tall column of weather and always has
	     been; landscape would be a wide column with nothing either side of it.
	     Better to not offer it than to offer it badly. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
</dict>
</plist>
