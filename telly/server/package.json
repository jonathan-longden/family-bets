{
  "name": "telly-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for the Telly IPTV clients: accounts, entitlements, playlists and streaming tickets.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test \"test/**/*.test.js\"",
    "admin": "node bin/telly-admin.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "better-sqlite3": "^11.3.0",
    "fastify": "^4.28.1"
  }
}
